import { ContentShell } from 'features/Content';

export const GuestHome = () => {
    return (
        <ContentShell>
            <p>Please connect your wallet as administrator, professor or student to see event details.</p>
        </ContentShell>
    )
}
